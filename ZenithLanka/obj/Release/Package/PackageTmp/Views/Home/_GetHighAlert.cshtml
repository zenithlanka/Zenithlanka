@model ZenithLanka.Models.Post

<div class="p-2">
    @if (Model == null)
    {
        <div class="card mb-2 text-center">
            <div class="h5">No Alert</div>
        </div>
    }
    @if (Model.Description != null)
    {
        <div class="card mb-2">
            <div class="p-2">
                @*<div class="h5">
                        @Html.DisplayFor(modelItem => item.Title)
                    </div>*@
                @*<small>@item.Datetime</small>*@
                @Html.Raw(Model.Description)
            </div>
        </div>
    }
    
</div>
